{"id":"jri-dyl.1","title":"Configure nginx and systemd for production deployment","description":"Set up nginx to proxy justralph.it to the uvicorn app, and create a systemd service for auto-start.\n\n## Nginx configuration\nUpdate /etc/nginx/sites-enabled/default (or the file containing the justralph.it server block) to proxy to the app:\n\n```nginx\nserver {\n    listen 80;\n    server_name justralph.it www.justralph.it;\n\n    # Proxy all requests to the FastAPI app\n    location / {\n        proxy_pass http://127.0.0.1:8000;\n        proxy_http_version 1.1;\n        proxy_set_header Upgrade $http_upgrade;\n        proxy_set_header Connection 'upgrade';\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n        \n        # SSE support: disable buffering\n        proxy_buffering off;\n        proxy_cache off;\n        proxy_read_timeout 86400s;  # 24 hours for long SSE connections\n        proxy_send_timeout 86400s;\n    }\n\n    # Keep the /prd redirect\n    location /prd {\n        return 301 https://nicolaspujia.com/ralph;\n    }\n}\n```\n\nImportant: Do NOT modify other server blocks in the nginx config (bot.nicolaspujia.com, podcast.nicolaspujia.com). Only change the justralph.it block. The static fallback is removed since the FastAPI app now serves the landing page directly.\n\n## Systemd service\nCreate /etc/systemd/system/jri.service:\n\n```ini\n[Unit]\nDescription=Just Ralph It\nAfter=network.target\n\n[Service]\nType=simple\nUser=nico\nWorkingDirectory=/home/nico/jri\nExecStart=/usr/bin/python3 -m uvicorn app.main:app --host 127.0.0.1 --port 8000\nRestart=always\nRestartSec=5\nEnvironment=PATH=/home/nico/.local/bin:/home/linuxbrew/.linuxbrew/bin:/usr/local/bin:/usr/bin:/bin\n\n[Install]\nWantedBy=multi-user.target\n```\n\n## Deployment steps (to be run as root or with sudo)\n1. Copy the nginx config.\n2. Test nginx config: nginx -t.\n3. Reload nginx: systemctl reload nginx.\n4. Copy the systemd service file.\n5. systemctl daemon-reload.\n6. systemctl enable jri.\n7. systemctl start jri.\n\n## Deployment script\nCreate deploy.sh in the project root that does all of the above:\n```bash\n#!/bin/bash\nset -e\npip install -r requirements.txt\nsudo cp deploy/nginx.conf /etc/nginx/sites-enabled/justralph.it\nsudo nginx -t\nsudo systemctl reload nginx\nsudo cp deploy/jri.service /etc/systemd/system/jri.service\nsudo systemctl daemon-reload\nsudo systemctl enable jri\nsudo systemctl restart jri\necho 'Deployed successfully'\n```\n\nStore the nginx and systemd config files in a deploy/ directory in the repo.","acceptance_criteria":"1. The file deploy/nginx.conf exists with the correct nginx server block configuration including SSE support headers.\n2. The file deploy/jri.service exists with the correct systemd service configuration.\n3. The deploy.sh script exists and is executable.\n4. Running deploy.sh installs dependencies, updates nginx config, and starts the service.\n5. After deployment, https://justralph.it serves the FastAPI app (landing page).\n6. SSE connections work through nginx (proxy_buffering off is set).\n7. The /prd redirect still works.\n8. Other nginx server blocks (bot.nicolaspujia.com, podcast) are NOT modified.\n9. The service auto-restarts on crash (Restart=always).\n10. The service auto-starts on server boot (WantedBy=multi-user.target).","status":"open","priority":0,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:54:33Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:54:33Z","dependencies":[{"issue_id":"jri-dyl.1","depends_on_id":"jri-3fb.5","type":"blocks","created_at":"2026-03-21T11:54:36Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-dyl.1","depends_on_id":"jri-dyl","type":"parent-child","created_at":"2026-03-21T11:54:32Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"jri-3fb.5","title":"Implement main project page with two-panel layout","description":"Create the main project page at GET /project/{name} with the two-panel layout.\n\n## Route: GET /project/{name} (in app/routers/pages.py)\n- Requires auth (redirect to / if not logged in).\n- Verify project belongs to user (404 if not).\n- Check for ?payment=success query param — if present, start Ralph loop via API call.\n- Render the project template with project data passed to the template.\n\n## Template: templates/project.html\nExtends base.html. This is the main workspace.\n\n### Layout (CSS Grid or Flexbox)\n- Full viewport height (100vh minus header).\n- Two panels side by side.\n- Left panel: 40% width. Right panel: 60% width.\n- A subtle vertical divider between them (1px border).\n\n### Header (same as dashboard but with additions)\n- 'Just Ralph It' (link to /dashboard) on left.\n- Project name in the center.\n- GitHub repo link (icon or text, opens new tab) next to project name.\n- User avatar + username + logout on the right.\n\n### Left Panel: Chat\nA chat interface with:\n\n#### Message display area\n- Scrollable div taking up all available height above the input.\n- Messages alternate between user (right-aligned, slightly different bg) and assistant (left-aligned).\n- User messages: rendered as plain text (but the user typed markdown, so show it raw).\n- Assistant messages: rendered as HTML from markdown (use a simple markdown-to-HTML function in JS — or just show raw text for v1 and iterate later. Decision: show raw text with basic formatting: bold, italic, code, links. Use a minimal regex-based renderer, not a full library).\n- Auto-scroll to bottom on new messages.\n- Show a 'Ralphy is thinking...' indicator when waiting for response.\n\n#### Input area (at bottom of left panel)\n- A textarea (not input) with placeholder 'Type your message... (markdown encouraged)'.\n- Below the textarea: a row with:\n  - An attachment button (paperclip icon or text 'Attach') that opens a file picker (accept='image/*,application/pdf', max 3 files, max 3MB each).\n  - Show attached file names if any.\n  - A 'Send' button.\n- The textarea should grow with content (auto-resize) up to 200px max height, then scroll.\n- Keyboard: Enter sends, Shift+Enter for newline.\n- **Disabled state**: while Ralphy is processing (SSE ralphy_processing event), disable the textarea and send button. Show 'Ralphy is thinking...' indicator.\n\n#### JRI button\n- A prominent button labeled 'Just Ralph It' that appears ABOVE the input area.\n- Visibility controlled by a JavaScript flag set from SSE or Ralphy's response.\n- When Ralphy's response includes the phrase 'Just Ralph It' button or similar indicator (we need a protocol — see below), show the button.\n- On click:\n  - If free tier applies: call POST /api/projects/{name}/ralph/checkout. If response.free is true, Ralph starts. Else redirect to Stripe.\n  - The button disappears after clicking.\n  - Show 'Stop' and 'Resume' buttons in its place.\n\n#### JRI button protocol\nRalphy doesn't directly control the UI. Instead, the backend detects when Ralphy has marked issues as ready (status changed from deferred to open). The detection logic:\n- After each Ralphy response, the frontend polls GET /api/projects/{name}/issues.\n- If there are issues with status='open' AND none with status='deferred', show the JRI button.\n- This way, Ralphy just needs to do its job (mark issues open) and the UI reacts.\n\n#### Stop/Resume buttons\n- 'Stop' button: visible when Ralph is running. On click: POST /api/projects/{name}/ralph/stop. Changes to 'Stopping...' until the loop actually stops.\n- 'Resume' button: visible when Ralph is stopped (not idle — stopped means intentionally paused). On click: POST /api/projects/{name}/ralph/start.\n\n### Right Panel: Tabs\nFour tabs with tab headers at the top of the right panel.\n\n#### Tab 1: Issues\n- Default active tab.\n- On load and periodically (via SSE issue_update events): fetch GET /api/projects/{name}/issues.\n- Render issues grouped by epic:\n  - Epic name as a section header with status indicator (colored dot: green=closed, yellow=in_progress, gray=open, dim=deferred).\n  - Under each epic: list of child issues, each showing:\n    - Status dot (same color coding)\n    - Issue ID (small, muted)\n    - Title\n    - Type badge (small, text: feat/task/bug/chore)\n    - Priority (P0-P4, only show if P0 or P1)\n  - Each issue is clickable to expand/collapse details:\n    - Description (full text)\n    - Acceptance criteria (full text)\n    - Dependencies (list of issue IDs)\n    - Assignee (if set)\n- Ungrouped issues at the bottom under 'Other'.\n- Issues should update in real-time via SSE without full re-render (update individual issue status dots and add new issues).\n\n#### Tab 2: AGENTS.md\n- Fetch GET /api/projects/{name}/agents-md on tab activation and on SSE agents_md_update events.\n- Render the markdown content. For v1, basic rendering: headings, paragraphs, lists, code blocks, bold, italic. Use the same minimal renderer as chat messages.\n- If AGENTS.md doesn't exist yet, show 'AGENTS.md will appear here as Ralphy defines the project structure.'\n\n#### Tab 3: Ralph (only visible after Ralph starts)\n- Tab header only appears when Ralph loop status is 'running', 'stopping', or 'stopped'.\n- Content: a terminal-like div with dark background (#111) and monospace font.\n- Connect to GET /api/projects/{name}/events SSE and filter for ralph_stdout events.\n- Append each stdout line to the terminal div.\n- Auto-scroll to bottom.\n- Show the current issue being worked on at the top: 'Working on: {issue_id} — {issue_title}'.\n- When loop is done, show 'Ralph has finished all issues.' at the bottom.\n\n#### Tab 4: Uploads\n- Fetch GET /api/projects/{name}/uploads on tab activation.\n- Show a file list: name, size (human-readable: KB/MB), modified date.\n- Each file has: a rename button (pencil icon) and a delete button (X icon).\n- Rename: inline edit — clicking rename turns the name into an input field. On blur or Enter: PATCH the API. On Escape: cancel.\n- Delete: confirm() dialog, then DELETE API call, remove from list.\n- Upload area: a 'Upload File' button that opens a file picker (no type/size constraints). On selection: POST /api/projects/{name}/uploads with the file. Add the new file to the list.\n- Drag-and-drop upload: the entire uploads tab area is a drop zone. On drop: upload all dropped files.\n\n### Notification banner\n- At the very top of the page (above header), a persistent yellow/orange banner.\n- Shows when there are unacknowledged notifications (from SSE notification events or fetched on page load).\n- Content: the notification message (e.g., 'Ralph needs help: Need GitHub API key for deployment').\n- An 'Acknowledge' button on the right of the banner. On click: POST acknowledge API, hide the banner.\n- If multiple notifications, stack them or show the most recent with a count.\n\n### Completion toast\n- When the Ralph loop finishes (SSE ralph_status event with status='done'):\n  - Show a toast notification in the bottom-right corner with a green background: 'Your project is ready!'\n  - Auto-dismiss after 10 seconds, or click to dismiss.\n  - The toast is just a positioned div with a fade-in/fade-out animation.\n\n### SSE connection\nOn page load, connect to GET /api/projects/{name}/events via EventSource.\nHandle all event types:\n- issue_update: refresh the issues tab data\n- agents_md_update: refresh the AGENTS.md tab if active\n- ralph_stdout: append to Ralph terminal\n- ralph_status: update loop state, show/hide tabs and buttons\n- notification: show notification banner\n- ralphy_processing: enable/disable chat input\n\n### Page load initialization\n1. Fetch GET /api/projects/{name} for project data (including ralph_loop_status).\n2. Fetch GET /api/projects/{name}/issues for initial issues.\n3. Fetch GET /api/projects/{name}/agents-md for initial AGENTS.md.\n4. Connect SSE.\n5. If ralph_loop_status is 'running': show Ralph tab, show Stop button, connect stdout stream.\n6. If ralph_loop_status is 'idle' and there are open issues but no deferred ones: show JRI button.\n7. Fetch GET /api/projects/{name}/notifications for any pending notifications.\n\nAll JavaScript is vanilla — no frameworks, no build step.","acceptance_criteria":"1. GET /project/{name} renders the two-panel layout with chat on the left and tabs on the right.\n2. The left panel has a textarea input with 'Type your message...' placeholder, Send button, and Attach button.\n3. Sending a message streams Ralphy's response in real-time (SSE), displayed in the chat area.\n4. The right panel has 4 tabs: Issues, AGENTS.md, Ralph, Uploads.\n5. Issues tab shows issues grouped by epic with status dots, expandable details.\n6. AGENTS.md tab renders the markdown content of the project's AGENTS.md.\n7. Uploads tab lists files with upload, rename, delete, and drag-and-drop functionality.\n8. Ralph tab appears when the loop is running and shows streaming stdout in a terminal-like view.\n9. The JRI button appears above the chat input when all issues are open and none are deferred.\n10. Clicking JRI triggers Stripe checkout (or free tier) and starts the Ralph loop.\n11. Stop/Resume buttons work correctly.\n12. Notification banner appears at the top when Ralph needs human help.\n13. Green 'Your project is ready!' toast appears when Ralph finishes all issues.\n14. SSE connection handles all event types and updates the UI in real-time.\n15. The page correctly initializes based on the current project state (running loop, idle, etc.).\n16. Enter sends message, Shift+Enter creates newline.\n17. Chat input is disabled while Ralphy is processing.\n18. No external JavaScript frameworks — all vanilla JS.\n19. The page has a link to the GitHub repo in the header.","status":"open","priority":0,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:54:04Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:54:04Z","dependencies":[{"issue_id":"jri-3fb.5","depends_on_id":"jri-0tq.2","type":"blocks","created_at":"2026-03-21T11:54:10Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-3fb.5","depends_on_id":"jri-2ti.1","type":"blocks","created_at":"2026-03-21T11:54:09Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-3fb.5","depends_on_id":"jri-3fb","type":"parent-child","created_at":"2026-03-21T11:54:04Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-3fb.5","depends_on_id":"jri-3fb.1","type":"blocks","created_at":"2026-03-21T11:54:09Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-3fb.5","depends_on_id":"jri-aor.1","type":"blocks","created_at":"2026-03-21T11:54:09Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-3fb.5","depends_on_id":"jri-aor.2","type":"blocks","created_at":"2026-03-21T11:54:09Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-3fb.5","depends_on_id":"jri-aor.3","type":"blocks","created_at":"2026-03-21T11:54:10Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-3fb.5","depends_on_id":"jri-ehk.1","type":"blocks","created_at":"2026-03-21T11:54:10Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-3fb.5","depends_on_id":"jri-qps.1","type":"blocks","created_at":"2026-03-21T11:54:10Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-3fb.5","depends_on_id":"jri-qps.2","type":"blocks","created_at":"2026-03-21T11:54:10Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":9,"dependent_count":1,"comment_count":0}
{"id":"jri-3fb.4","title":"Implement project creation page","description":"Create the project creation page at GET /new.\n\n## Route: GET /new (in app/routers/pages.py)\n- Requires auth (redirect to / if not logged in).\n- Render the project creation template.\n\n## Template: templates/new_project.html\nExtends base.html. Same header as dashboard. Contains:\n\n### Content\n- h2: 'New Project'\n- A form with:\n  - Label 'Project Name' + text input (name='name'). Validation: alphanumeric + hyphens, 1-100 chars. Show validation inline.\n  - Label 'Description' + textarea (name='description', placeholder='What do you want to build?', rows=4).\n  - Submit button: 'Create Project'\n- On submit (JavaScript):\n  - Prevent default.\n  - Validate name client-side (regex: ^[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9]$|^[a-zA-Z0-9]$ for single char).\n  - Show a loading state on the button ('Creating...', disabled).\n  - POST /api/projects with JSON {name, description}.\n  - On success: redirect to /project/{name}.\n  - On 400/409: show the error message below the name field.\n  - On other errors: show a generic error message.\n\n### Styles\n- Form max-width 500px, centered.\n- Input and textarea: full width, dark theme (dark bg, light border, light text).\n- Error messages in red.","acceptance_criteria":"1. GET /new when logged in renders the form with name input, description textarea, and submit button.\n2. GET /new when not logged in redirects to /.\n3. Submitting valid name and description creates the project and redirects to /project/{name}.\n4. Submitting an invalid name (e.g., '---') shows an inline validation error without calling the API.\n5. Submitting a duplicate name shows the 409 error message from the API.\n6. The submit button shows 'Creating...' and is disabled during the API call.\n7. The description textarea placeholder is 'What do you want to build?'.\n8. Form styling matches the dark theme.","status":"open","priority":0,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:52:55Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:52:55Z","dependencies":[{"issue_id":"jri-3fb.4","depends_on_id":"jri-3fb","type":"parent-child","created_at":"2026-03-21T11:52:54Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-3fb.4","depends_on_id":"jri-3fb.1","type":"blocks","created_at":"2026-03-21T11:52:58Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-3fb.4","depends_on_id":"jri-upe.1","type":"blocks","created_at":"2026-03-21T11:52:58Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"id":"jri-3fb.3","title":"Implement dashboard page","description":"Create the dashboard page at GET /dashboard.\n\n## Route: GET /dashboard (in app/routers/pages.py)\n- Requires auth (redirect to / if not logged in).\n- Render the dashboard template.\n\n## Template: templates/dashboard.html\nExtends base.html. Contains:\n\n### Header\n- 'Just Ralph It' text (link to /dashboard) on the left\n- User's GitHub avatar (32x32, rounded) and username on the right, with a 'Logout' link to /auth/logout\n\n### Content\n- h2: 'Your Projects'\n- A 'New Project' button (link to /new) — simple bordered button\n- A list of project cards, loaded via JavaScript fetch to GET /api/projects\n\n### Project card\nEach project is a div with:\n- Project name (link to /project/{name})\n- Issue count: '{n} issues'\n- GitHub repo link (external, opens in new tab)\n- Delete button (small, red text) — on click: shows confirm dialog\n\n### Delete confirmation\nUse native confirm() dialog. On confirm: fetch DELETE /api/projects/{name}?delete_repo=true, remove the card from DOM.\n\n### Empty state\nIf no projects: 'No projects yet.' centered, with 'New Project' button below.\n\n### JavaScript\nVanilla JS only. On load: fetch GET /api/projects and render. Delete handler as described.","acceptance_criteria":"1. GET /dashboard when logged in renders the page with header showing user's avatar and username.\n2. GET /dashboard when not logged in redirects to /.\n3. The page fetches and displays projects with name, issue count, and GitHub link.\n4. Each project name links to /project/{name}.\n5. The 'New Project' button links to /new.\n6. Clicking delete shows confirm dialog and on confirm calls DELETE API and removes card.\n7. When no projects, 'No projects yet.' is displayed.\n8. Logout link works and redirects to landing page.\n9. No external JavaScript frameworks — vanilla JS only.","status":"open","priority":0,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:52:36Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:52:36Z","dependencies":[{"issue_id":"jri-3fb.3","depends_on_id":"jri-3fb","type":"parent-child","created_at":"2026-03-21T11:52:36Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-3fb.3","depends_on_id":"jri-3fb.1","type":"blocks","created_at":"2026-03-21T11:52:41Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-3fb.3","depends_on_id":"jri-upe.2","type":"blocks","created_at":"2026-03-21T11:52:42Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"id":"jri-3fb.1","title":"Implement landing page","description":"Create the landing page at GET / served via Jinja2 template.\n\n## Route: GET / (in app/routers/pages.py)\n- If user is already logged in (valid session cookie), redirect to /dashboard.\n- Otherwise, render the landing page template.\n\n## Template: templates/landing.html\nA minimal HTML page with:\n- Title tag: 'Just Ralph It'\n- A centered container with:\n  - h1: 'Just Ralph It'\n  - p (NOT h2, a regular paragraph): 'Tool to transform your software idea into a very detailed plan and let an AI agent build it entirely.'\n  - A link styled as a button pointing to /auth/login with text 'Sign in with GitHub'\n- Minimal CSS inline or in a style tag in the head. No external CSS framework.\n  - System font stack: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif\n  - Dark background (#0a0a0a), light text (#e0e0e0)\n  - Container centered vertically and horizontally\n  - Button: simple border, no fill, white text, hover: light fill\n  - Max-width 600px for the container\n- No JavaScript needed on this page.\n\n## Template: templates/base.html\nCreate a base template that all pages extend:\n- DOCTYPE html, utf-8 charset, viewport meta tag\n- Block: title, head, content\n- Common styles in the head: the dark theme colors, system font, box-sizing border-box, margin/padding reset.","acceptance_criteria":"1. GET / when not logged in renders the landing page with the heading, paragraph, and GitHub button.\n2. GET / when logged in (valid session cookie) redirects to /dashboard.\n3. The page has a dark background (#0a0a0a) and light text (#e0e0e0).\n4. The 'Sign in with GitHub' link points to /auth/login.\n5. The page is centered vertically and horizontally.\n6. The paragraph text is exactly: 'Tool to transform your software idea into a very detailed plan and let an AI agent build it entirely.'\n7. A base.html template exists and is extended by landing.html.\n8. No external CSS frameworks or JavaScript libraries are loaded.","status":"open","priority":0,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:52:11Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:52:11Z","dependencies":[{"issue_id":"jri-3fb.1","depends_on_id":"jri-3fb","type":"parent-child","created_at":"2026-03-21T11:52:10Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-3fb.1","depends_on_id":"jri-6j8.1","type":"blocks","created_at":"2026-03-21T11:52:10Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":1,"dependent_count":3,"comment_count":0}
{"id":"jri-3fb","title":"Frontend UI","description":"All frontend pages: landing, dashboard, project creation, and the main two-panel project page.","acceptance_criteria":"All 4 pages are functional, minimal, and intuitive. No fancy styling — clean and readable. All pages work behind nginx at justralph.it.","status":"open","priority":0,"issue_type":"epic","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:51:55Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:51:55Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"jri-qps.2","title":"Implement SSE event bus for real-time updates","description":"Create a centralized SSE event bus in app/sse_bus.py that the frontend connects to for all real-time updates.\n\n## GET /api/projects/{name}/events (in app/routers/sse.py)\n- Requires auth. Verify project belongs to user.\n- Returns text/event-stream.\n- The client connects once and receives ALL event types for this project:\n  - `issue_update`: an issue was created, updated, or its status changed\n  - `agents_md_update`: AGENTS.md was modified\n  - `ralph_stdout`: a line of Ralph's output\n  - `ralph_status`: Ralph loop status changed (started, stopped, done, crash_recovery)\n  - `notification`: a notification for the user (Ralph needs help)\n  - `ralphy_processing`: Ralphy started/finished processing (for disabling input)\n\n## app/sse_bus.py\n\n### Class: SSEBus\nA singleton per-project event bus.\n\n```python\nclass SSEBus:\n    def __init__(self):\n        self._subscribers: dict[int, set[asyncio.Queue]] = {}  # project_id -\u003e set of queues\n    \n    def subscribe(self, project_id: int) -\u003e asyncio.Queue:\n        queue = asyncio.Queue(maxsize=100)\n        self._subscribers.setdefault(project_id, set()).add(queue)\n        return queue\n    \n    def unsubscribe(self, project_id: int, queue: asyncio.Queue):\n        self._subscribers.get(project_id, set()).discard(queue)\n    \n    async def publish(self, project_id: int, event_type: str, data: dict):\n        for queue in self._subscribers.get(project_id, set()):\n            try:\n                queue.put_nowait({\"event\": event_type, \"data\": data})\n            except asyncio.QueueFull:\n                pass  # Drop old events if client is slow\n\nsse_bus = SSEBus()  # Singleton\n```\n\n### SSE endpoint implementation\n```python\n@router.get('/api/projects/{name}/events')\nasync def project_events(name: str, user=Depends(get_current_user)):\n    project = await get_project(user, name)\n    queue = sse_bus.subscribe(project['id'])\n    \n    async def event_generator():\n        try:\n            while True:\n                event = await asyncio.wait_for(queue.get(), timeout=30)\n                yield f'event: {event[\"event\"]}\\ndata: {json.dumps(event[\"data\"])}\\n\\n'\n        except asyncio.TimeoutError:\n            yield f': keepalive\\n\\n'  # Send keepalive comment\n        except asyncio.CancelledError:\n            pass\n        finally:\n            sse_bus.unsubscribe(project['id'], queue)\n    \n    return StreamingResponse(event_generator(), media_type='text/event-stream')\n```\n\n### Integration points\n- The RalphLoop publishes ralph_stdout and ralph_status events.\n- The chat endpoint publishes ralphy_processing events (start/end).\n- A background polling task publishes issue_update events when bd state changes.\n- The notifications system publishes notification events.\n\n### Background issue poller\nCreate a background task that, for each project with an active Ralph loop, polls `bd list --json` every 3 seconds and compares with the previous state. If any issue's status changed or new issues appeared, publish issue_update events. Also poll AGENTS.md content (hash comparison) and publish agents_md_update if changed.","acceptance_criteria":"1. GET /api/projects/{name}/events returns a text/event-stream response.\n2. When Ralph publishes a stdout line, connected SSE clients receive it as a ralph_stdout event.\n3. When an issue status changes, connected SSE clients receive an issue_update event.\n4. When AGENTS.md changes, connected SSE clients receive an agents_md_update event.\n5. SSE connection sends keepalive comments every 30 seconds to prevent timeout.\n6. Multiple SSE clients can connect to the same project and all receive the same events.\n7. Disconnecting a client cleans up the subscription (no memory leak).\n8. The background poller runs every 3 seconds for projects with active Ralph loops.\n9. The SSE bus is a singleton — all parts of the app publish to the same instance.","status":"open","priority":0,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:50:59Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:50:59Z","dependencies":[{"issue_id":"jri-qps.2","depends_on_id":"jri-edz.1","type":"blocks","created_at":"2026-03-21T11:50:59Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-qps.2","depends_on_id":"jri-qps","type":"parent-child","created_at":"2026-03-21T11:50:59Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"id":"jri-qps.1","title":"Implement Ralph loop subprocess manager","acceptance_criteria":"1. POST /api/projects/{name}/ralph/start starts the Ralph loop and returns 200.\n2. GET /api/projects/{name}/ralph/status returns the current loop status, issue, and iteration.\n3. GET /api/projects/{name}/ralph/stream returns an SSE stream that includes Ralph's claude CLI output in real-time.\n4. POST /api/projects/{name}/ralph/stop sets the loop to 'stopping' and it stops after the current iteration finishes.\n5. If the claude subprocess crashes (non-zero exit), the loop recovers: git reset --hard, issue reopened, loop continues.\n6. The loop state is saved to .ralph_state before each iteration.\n7. When there are no more ready issues, the loop status becomes 'idle' and subscribers receive a loop_done event.\n8. POST /api/projects/{name}/ralph/start on an already-running loop returns 409.\n9. Ralph's system prompt is stored in app/prompts/ralph.py and contains all 14 rules listed above.\n10. The build_ralph_prompt function constructs a complete prompt from the issue JSON including Co-authored-by trailer information.","notes":"IMPORTANT: The bd ready command must filter out epics. Use: bd ready -n 1 --json then filter the result to exclude issues where issue_type == 'epic'. Alternatively, Ralph's loop can try bd ready -n 1 -t task --json first, then bd ready -n 1 -t feature --json, then bd ready -n 1 -t bug --json, etc. The simplest approach: run bd ready -n 1 --json, parse the result, skip if it's an epic (just close it and continue).","status":"open","priority":0,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:50:36Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:55:49Z","dependencies":[{"issue_id":"jri-qps.1","depends_on_id":"jri-2ti.1","type":"blocks","created_at":"2026-03-21T11:50:35Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-qps.1","depends_on_id":"jri-qps","type":"parent-child","created_at":"2026-03-21T11:50:35Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":1,"dependent_count":3,"comment_count":0}
{"id":"jri-aor.1","title":"Implement beads issues polling API endpoint","description":"Create an API endpoint that returns the current state of beads issues for a project.\n\n## GET /api/projects/{name}/issues\n- Requires auth. Verify project belongs to user.\n- Run `bd list --json` in the project directory.\n- Parse the JSON output (array of issue objects).\n- Group issues by their parent epic. Issues without a parent go in an 'Ungrouped' section.\n- For each issue, also fetch dependency info by examining the deps field in the bd output.\n- Return JSON structure:\n```json\n{\n  \"epics\": [\n    {\n      \"id\": \"jri-abc\",\n      \"title\": \"Epic Name\",\n      \"status\": \"open\",\n      \"children\": [\n        {\n          \"id\": \"jri-abc.1\",\n          \"title\": \"Task Name\",\n          \"type\": \"feature\",\n          \"status\": \"deferred\",\n          \"priority\": 2,\n          \"description\": \"...\",\n          \"acceptance_criteria\": \"...\",\n          \"assignee\": null,\n          \"dependencies\": [\"jri-abc.0\"],\n          \"created_at\": \"...\"\n        }\n      ]\n    }\n  ],\n  \"ungrouped\": [...]\n}\n```\n- If bd list fails (e.g., beads not initialized), return {\"epics\": [], \"ungrouped\": []}.\n\n## GET /api/projects/{name}/issues/{issue_id}\n- Requires auth.\n- Run `bd show {issue_id} --json` in the project directory.\n- Return the full issue JSON as-is from bd.","acceptance_criteria":"1. GET /api/projects/{name}/issues returns a JSON object with 'epics' and 'ungrouped' arrays.\n2. Issues are grouped under their parent epic correctly.\n3. Each issue contains id, title, type, status, priority, description, acceptance_criteria, assignee, dependencies, and created_at.\n4. GET /api/projects/{name}/issues/{id} returns the full details of a specific issue.\n5. For a project with no issues, returns {\"epics\": [], \"ungrouped\": []}.\n6. For an invalid project name, returns 404.","status":"open","priority":0,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:49:21Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:49:21Z","dependencies":[{"issue_id":"jri-aor.1","depends_on_id":"jri-aor","type":"parent-child","created_at":"2026-03-21T11:49:20Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-aor.1","depends_on_id":"jri-upe.1","type":"blocks","created_at":"2026-03-21T11:49:20Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"jri-2ti.1","title":"Implement Ralphy chat backend using claude CLI subprocess","description":"Implement the chat endpoint that proxies messages to/from the claude CLI in app/routers/chat.py.\n\n## POST /api/projects/{name}/chat\nRequest body: { \"message\": string }\nResponse: SSE stream (text/event-stream)\n\n### Behavior\n1. Requires auth. Verify project belongs to user.\n2. Get or create a Ralphy session for this project:\n   - If project.ralph_session_id is NULL: generate a new UUID v4, store it in the DB as ralph_session_id. This is the first message.\n   - If project.ralph_session_id is set: this is a continuation.\n3. Run the claude CLI as an async subprocess:\n   - First message: `claude -p --model opus --session-id {session_id} --output-format stream-json --verbose --dangerously-skip-permissions --system-prompt {ralphy_system_prompt} --allowedTools 'Bash(bd:*) Bash(git:*) Read Write Edit Glob Grep' -- '{user_message}'`\n   - Continuation: `claude -p --model opus --resume {session_id} --output-format stream-json --verbose --dangerously-skip-permissions --allowedTools 'Bash(bd:*) Bash(git:*) Read Write Edit Glob Grep' -- '{user_message}'`\n   - Set cwd to the project directory.\n   - Set environment variable BD_ACTOR=ralphy for beads audit trail.\n4. Stream the subprocess stdout line by line. Each line is a JSON object from claude's stream-json output format.\n5. For each line, parse the JSON and extract relevant events:\n   - type='assistant' with content: extract text parts and stream as SSE `data: {\"type\": \"text\", \"content\": \"...\"}`\n   - type='assistant' with tool_use: stream as SSE `data: {\"type\": \"tool_use\", \"name\": \"...\", \"input\": ...}`\n   - type='result': stream as SSE `data: {\"type\": \"done\", \"result\": \"...\"}` and close.\n6. If the subprocess exits with non-zero, stream SSE `data: {\"type\": \"error\", \"message\": \"...\"}`.\n\n### The Ralphy system prompt\nStore in app/prompts/ralphy.py as a constant string. Content (this is critical — it defines the product):\n\n```\nYou are Ralphy, an AI assistant that helps users transform their software ideas into extremely detailed, unambiguous implementation plans.\n\nYou work inside a project directory that has beads (bd) initialized. You create and manage beads issues that will be consumed by Ralph, an autonomous coding agent that works in a fresh context per issue with ZERO access to this conversation.\n\n## Your personality\n- Neutral, extremely persistent, and patient\n- You push back on ideas when warranted — state trade-offs of alternatives, but leave the final decision to the user\n- It is a completely valid outcome if the user realizes they don't want to build the project\n- You NEVER rush to conclusions or skip details\n\n## Your workflow\n1. START by understanding the PROBLEM and INTENT. Ask: What problem does this solve? Who is it for? Why does it need to exist?\n2. Once intent is clear, explore the solution space. Ask about features, user flows, edge cases.\n3. For tech stack: ask the user if they want to discuss it. If they say no, you decide the simplest/best stack for the job. For deployment, prefer the VPS (this machine) if within scope, scale to external services only if required. Not all projects are web dev.\n4. Create beads issues INCREMENTALLY as topics are covered. Each issue starts in DEFERRED status.\n5. Manage dependencies between issues using bd dep add.\n6. Generate and maintain the root AGENTS.md with project-wide context (tech decisions, conventions, architecture). Anything that belongs in a specific issue stays in that issue. AGENTS.md is for cross-cutting concerns only.\n7. Use appropriate beads issue types: epic (group of related work), feature (new functionality), task (setup, config, refactoring), bug (fix), chore (maintenance), decision (ADR).\n8. Each issue MUST have:\n   - A clear, specific title\n   - A detailed description explaining WHAT to build and HOW\n   - Acceptance criteria that are testable and have exactly ONE interpretation\n   - Correct dependencies (what must be done before this issue)\n9. When you believe the issues are comprehensive enough to build the entire project, tell the user and ask them to confirm. Before marking issues as ready:\n   - Spin a subagent (via the Task tool) to review each issue and try to find ambiguities — places where the acceptance criteria could be interpreted in more than one way. The subagent should also check that dependencies are correct and complete.\n   - Resolve any flagged ambiguities with the user.\n   - Then mark all confirmed issues from deferred to open status using: bd update {id} --status open\n10. After marking issues as ready, tell the user: \"The issues are ready. You can click 'Just Ralph It' to start building.\"\n\n## CRITICAL RULES\n- Issues must be COMPLETELY unambiguous. Ralph has NO access to this conversation. Every detail Ralph needs must be in the issue description, acceptance criteria, or AGENTS.md.\n- NEVER let a product decision go unresolved. If there's a choice to make, ask the user.\n- DO NOT include placeholder implementations in issue descriptions. Describe the FULL, COMPLETE behavior.\n- Ralph follows TDD: write tests from acceptance criteria first, then implement. Write acceptance criteria with this in mind.\n- The user may upload files to the uploads/ directory. Any files the human has provided are there.\n- When the user sends new messages while Ralph is working, create new issues in DEFERRED status. If an issue hasn't been started by Ralph yet, you may edit it — but be very careful not to break the project structure by editing one issue.\n- Briefly communicate your notes and decisions to the user in chat. Don't be silent about what you're doing.\n\n## bd commands you can use\n- bd create \"Title\" -t type -p priority -d \"description\" --acceptance \"criteria\" --parent epic-id --deps \"dep-id\"\n- bd update {id} --status open|deferred|blocked --title \"new title\" -d \"new desc\" --acceptance \"new criteria\"\n- bd dep add {child-id} {parent-id} (child depends on parent)\n- bd dep {blocker-id} --blocks {blocked-id}\n- bd list --json\n- bd show {id} --json\n- bd close {id}\n```\n\n### Important implementation notes\n- The system prompt must be passed as a file to avoid shell escaping issues. Write it to a temp file and use `--system-prompt-file` if available, or write it to the project's .ralphy_prompt file and read it.\n- Actually: claude CLI does not have --system-prompt-file. So write the prompt to {project_dir}/.ralphy_system_prompt and use `--system-prompt \"$(cat {project_dir}/.ralphy_system_prompt)\"`. Wait — that's still shell. Instead, construct the subprocess args list in Python and pass the prompt as a string argument directly (no shell=True). This avoids all escaping issues.\n- Always use subprocess with shell=False (pass args as a list).","acceptance_criteria":"1. POST /api/projects/{name}/chat with {\"message\": \"Hello\"} returns a streaming SSE response (Content-Type: text/event-stream).\n2. The SSE stream contains events with type='text' that include Ralphy's response text.\n3. The SSE stream ends with a type='done' event.\n4. A second POST to the same project continues the same claude session (conversation history is preserved — Ralphy remembers what was said before).\n5. The claude subprocess runs with cwd set to the project directory.\n6. The claude subprocess has --dangerously-skip-permissions so it can execute bd commands without prompts.\n7. The claude subprocess has --allowedTools restricting it to Bash(bd:*), Bash(git:*), Read, Write, Edit, Glob, Grep.\n8. Sending a message to a project that doesn't belong to the user returns 404.\n9. The Ralphy system prompt is stored as a constant in app/prompts/ralphy.py.\n10. The system prompt contains all the behavioral instructions listed in the description above.","status":"open","priority":0,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:48:50Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:48:50Z","dependencies":[{"issue_id":"jri-2ti.1","depends_on_id":"jri-2ti","type":"parent-child","created_at":"2026-03-21T11:48:50Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-2ti.1","depends_on_id":"jri-upe.1","type":"blocks","created_at":"2026-03-21T11:48:50Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":1,"dependent_count":3,"comment_count":0}
{"id":"jri-upe.2","title":"Implement project listing and deletion endpoints","description":"Add list and delete endpoints in app/routers/projects.py.\n\n## GET /api/projects\n- Requires auth.\n- Query all projects for the current user from SQLite.\n- For each project, count the number of beads issues by running `bd list --json` in the project directory and counting the returned array length. If the project directory doesn't exist or bd fails, return 0.\n- Return JSON array: [{ \"id\": ..., \"name\": ..., \"description\": ..., \"github_repo_url\": ..., \"issue_count\": ..., \"ralph_loop_status\": ..., \"created_at\": ... }, ...]\n\n## DELETE /api/projects/{name}\n- Requires auth.\n- Query param: `delete_repo` (boolean, default true).\n- Verify the project belongs to the current user. Return 404 if not found.\n- If ralph_loop_status is 'running', return 409 with message 'Cannot delete project while Ralph is running'.\n- If delete_repo is true: DELETE https://api.github.com/repos/ralphpujia/{name} using the ralphpujia token. Ignore 404 (repo may already be deleted).\n- Delete the project directory recursively: shutil.rmtree(project_path).\n- Delete the project row from SQLite (CASCADE will delete notifications too).\n- Return 204 No Content.\n\n## GET /api/projects/{name}\n- Requires auth.\n- Verify the project belongs to the current user. Return 404 if not found.\n- Return the project JSON (same shape as list items) plus: ralph_session_id, ralph_loop_current_issue, ralph_loop_iteration.","acceptance_criteria":"1. GET /api/projects returns an array of the user's projects with issue_count populated.\n2. GET /api/projects/{name} returns the full project details for an existing project.\n3. GET /api/projects/nonexistent returns 404.\n4. DELETE /api/projects/{name}?delete_repo=true deletes the local directory, the SQLite row, and the GitHub repo.\n5. DELETE /api/projects/{name}?delete_repo=false deletes the local directory and SQLite row but leaves the GitHub repo.\n6. DELETE on a project with ralph_loop_status='running' returns 409.\n7. After deletion, GET /api/projects no longer includes the deleted project.","status":"open","priority":0,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:47:53Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:47:53Z","dependencies":[{"issue_id":"jri-upe.2","depends_on_id":"jri-upe","type":"parent-child","created_at":"2026-03-21T11:47:53Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-upe.2","depends_on_id":"jri-upe.1","type":"blocks","created_at":"2026-03-21T11:47:53Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"jri-upe.1","title":"Implement project creation with GitHub repo initialization","description":"Implement project creation in app/routers/projects.py.\n\n## POST /api/projects\nRequest body (JSON): { \"name\": string, \"description\": string }\n\n### Validation\n- `name` must be 1-100 chars, alphanumeric + hyphens only, no leading/trailing hyphens. Return 400 with message if invalid.\n- `name` must be unique for the user. Return 409 if duplicate.\n\n### Steps (in order)\n1. Create the project directory at ~/jri/data/{github_username}/{name}/\n2. Initialize a git repo: `git init` in that directory.\n3. Set git user config for the repo: `git config user.name 'ralphpujia'` and `git config user.email 'ralphpujia@users.noreply.github.com'`.\n4. Initialize beads: `bd init` in that directory.\n5. Create an initial AGENTS.md in the project root with content:\n```markdown\n# {name}\n\n{description}\n\n## Project Info\n- Repository: https://github.com/ralphpujia/{name}\n- Created by: {github_username}\n```\n6. Create the uploads/ directory inside the project.\n7. Git add all, commit with message 'Initial project setup' and Co-authored-by header using the user's GitHub name and email (format: `Co-authored-by: {name} \u003c{email}\u003e`). If name or email is null, use github_username and `{github_username}@users.noreply.github.com`.\n8. Create the GitHub repo via GitHub API: POST https://api.github.com/user/repos with {\"name\": name, \"description\": description, \"private\": false, \"auto_init\": false}. Use the ralphpujia token (from config.RALPH_BOT_GITHUB_TOKEN) in the Authorization header.\n9. Add the user as a collaborator: PUT https://api.github.com/repos/ralphpujia/{name}/collaborators/{github_username} with {\"permission\": \"push\"}. Use the ralphpujia token.\n10. Add the GitHub remote: `git remote add origin https://x-access-token:{ralphpujia_token}@github.com/ralphpujia/{name}.git`\n11. Push: `git push -u origin main`.\n12. Insert the project into SQLite with github_repo_url = 'https://github.com/ralphpujia/{name}'.\n13. Return JSON: { \"id\": ..., \"name\": ..., \"description\": ..., \"github_repo_url\": ... }\n\n### Error handling\n- If any GitHub API call fails, clean up: delete the local directory and return 500 with the error message.\n- If the GitHub repo already exists (409 from GitHub), return 409 with message 'Repository already exists on GitHub'.\n\nAll subprocess calls (git, bd) must be run with cwd set to the project directory. Use asyncio.create_subprocess_exec for async subprocess execution.","acceptance_criteria":"1. POST /api/projects with {\"name\": \"test-project\", \"description\": \"A test\"} returns 200 with the project JSON including github_repo_url.\n2. The directory ~/jri/data/{username}/test-project/ exists and contains: .git/, .beads/, AGENTS.md, uploads/.\n3. The GitHub repo https://github.com/ralphpujia/test-project exists and is public.\n4. The user's GitHub account is listed as a collaborator on the repo.\n5. `git log` in the project directory shows the initial commit with the Co-authored-by header.\n6. bd list in the project directory works (beads is initialized).\n7. POST /api/projects with the same name returns 409.\n8. POST /api/projects with name '---invalid' returns 400.\n9. The project row exists in SQLite with correct github_repo_url.\n10. AGENTS.md contains the project name, description, repo URL, and creator username.","status":"open","priority":0,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:47:39Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:47:39Z","dependencies":[{"issue_id":"jri-upe.1","depends_on_id":"jri-6j8.1","type":"blocks","created_at":"2026-03-21T11:47:38Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-upe.1","depends_on_id":"jri-upe","type":"parent-child","created_at":"2026-03-21T11:47:38Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":1,"dependent_count":6,"comment_count":0}
{"id":"jri-6j8.1","title":"Implement GitHub OAuth login and session management","description":"Implement GitHub OAuth in app/routers/auth.py and session middleware.\n\n## OAuth Flow\n\n### GET /auth/login\n1. Generate a random `state` string (32 hex chars).\n2. Store it in a temporary signed cookie (`oauth_state`, max_age=300 seconds).\n3. Redirect to: `https://github.com/login/oauth/authorize?client_id={GITHUB_CLIENT_ID}\u0026redirect_uri=https://justralph.it/auth/callback\u0026scope=read:user,user:email\u0026state={state}`\n\n### GET /auth/callback?code=...\u0026state=...\n1. Verify `state` matches the `oauth_state` cookie. If not, return 400.\n2. Exchange `code` for access token: POST `https://github.com/login/oauth/access_token` with client_id, client_secret, code. Accept: application/json.\n3. Use the access token to GET `https://api.github.com/user` (with Authorization: Bearer header).\n4. Upsert the user in SQLite: INSERT OR REPLACE into users table using github_id as the unique key. Store github_username, github_name, github_email, github_avatar_url, and the access_token (as github_token).\n5. Set a signed cookie `session` containing the user's id (integer), using itsdangerous.URLSafeTimedSerializer with SECRET_KEY. Max age: 30 days.\n6. Delete the `oauth_state` cookie.\n7. Redirect to /dashboard.\n\n### GET /auth/logout\n1. Delete the `session` cookie.\n2. Redirect to /.\n\n### GET /auth/me (JSON API)\n1. Read the `session` cookie, deserialize with itsdangerous (max_age=30 days).\n2. Look up the user by id in SQLite.\n3. Return JSON: {id, github_username, github_name, github_avatar_url}. \n4. If no valid session, return 401.\n\n## Session middleware\nCreate a dependency `get_current_user(request: Request) -\u003e dict` in app/auth_utils.py that:\n1. Reads and deserializes the `session` cookie.\n2. Queries the user from SQLite.\n3. Returns the user row as a dict.\n4. Raises HTTPException(401) if cookie is missing, expired, or user not found.\n\nAll protected endpoints use `Depends(get_current_user)`.\n\n## HTTP client\nUse httpx.AsyncClient for all HTTP calls to GitHub. Create a shared instance in config.py or use a lifespan-managed client.","acceptance_criteria":"1. Visiting /auth/login in a browser redirects to GitHub's OAuth page with the correct client_id and callback URL.\n2. After authorizing on GitHub, the browser is redirected to /auth/callback, which sets a `session` cookie and redirects to /dashboard.\n3. GET /auth/me returns JSON with the user's GitHub username and avatar URL.\n4. GET /auth/me without a session cookie returns 401.\n5. GET /auth/logout deletes the session cookie and redirects to /.\n6. The user's GitHub token, username, name, email, and avatar URL are stored in the SQLite users table.\n7. Logging in a second time updates the existing user row (upsert), not creating a duplicate.\n8. The session cookie is signed — tampering with it causes a 401 on /auth/me.","status":"open","priority":0,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:47:03Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:47:03Z","dependencies":[{"issue_id":"jri-6j8.1","depends_on_id":"jri-6j8","type":"parent-child","created_at":"2026-03-21T11:47:03Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-6j8.1","depends_on_id":"jri-edz.2","type":"blocks","created_at":"2026-03-21T11:47:03Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"id":"jri-edz.2","title":"Create SQLite database schema and initialization","description":"Implement the SQLite database layer in app/database.py.\n\n## Schema\n\n### Table: users\n| Column | Type | Constraints |\n|--------|------|-------------|\n| id | INTEGER | PRIMARY KEY AUTOINCREMENT |\n| github_id | INTEGER | UNIQUE NOT NULL |\n| github_username | TEXT | NOT NULL |\n| github_name | TEXT | nullable |\n| github_email | TEXT | nullable |\n| github_avatar_url | TEXT | nullable |\n| github_token | TEXT | NOT NULL (the user's OAuth access token) |\n| created_at | TEXT | NOT NULL DEFAULT (datetime('now')) |\n\n### Table: projects\n| Column | Type | Constraints |\n|--------|------|-------------|\n| id | INTEGER | PRIMARY KEY AUTOINCREMENT |\n| user_id | INTEGER | NOT NULL REFERENCES users(id) ON DELETE CASCADE |\n| name | TEXT | NOT NULL |\n| description | TEXT | NOT NULL |\n| github_repo_url | TEXT | nullable |\n| ralph_session_id | TEXT | nullable (claude CLI session ID for Ralphy) |\n| ralph_loop_status | TEXT | NOT NULL DEFAULT 'idle' (one of: idle, running, stopping, stopped) |\n| ralph_loop_current_issue | TEXT | nullable (beads issue ID currently being worked) |\n| ralph_loop_iteration | INTEGER | NOT NULL DEFAULT 0 |\n| stripe_payment_id | TEXT | nullable |\n| created_at | TEXT | NOT NULL DEFAULT (datetime('now')) |\n| UNIQUE(user_id, name) |\n\n### Table: notifications\n| Column | Type | Constraints |\n|--------|------|-------------|\n| id | INTEGER | PRIMARY KEY AUTOINCREMENT |\n| project_id | INTEGER | NOT NULL REFERENCES projects(id) ON DELETE CASCADE |\n| message | TEXT | NOT NULL |\n| beads_issue_id | TEXT | nullable (the Human-assigned blocking issue) |\n| acknowledged | INTEGER | NOT NULL DEFAULT 0 |\n| created_at | TEXT | NOT NULL DEFAULT (datetime('now')) |\n\n## Implementation in app/database.py\n\n- `async def init_db()`: Create the database file at DATA_DIR/jri.db if it doesn't exist. Execute CREATE TABLE IF NOT EXISTS for all three tables. Enable WAL mode and foreign keys.\n- `async def get_db()`: Async context manager that yields an aiosqlite connection with row_factory = aiosqlite.Row.\n- All functions use the DATA_DIR/jri.db path from config.py.\n\n## Lifespan integration\n- In app/main.py, call `await init_db()` inside the lifespan startup.","acceptance_criteria":"1. Running the app and then checking: `sqlite3 ~/jri/data/jri.db '.tables'` shows 'users projects notifications'.\n2. Running `sqlite3 ~/jri/data/jri.db '.schema users'` shows the exact columns and constraints listed above.\n3. Running `sqlite3 ~/jri/data/jri.db '.schema projects'` shows the exact columns and constraints listed above, including the UNIQUE constraint on (user_id, name).\n4. Running `sqlite3 ~/jri/data/jri.db '.schema notifications'` shows the exact columns and constraints listed above.\n5. Running `sqlite3 ~/jri/data/jri.db 'PRAGMA journal_mode'` returns 'wal'.\n6. Running `sqlite3 ~/jri/data/jri.db 'PRAGMA foreign_keys'` returns '1'.\n7. The init_db function is idempotent — running it twice does not error or duplicate tables.","status":"open","priority":0,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:46:42Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:46:42Z","dependencies":[{"issue_id":"jri-edz.2","depends_on_id":"jri-edz","type":"parent-child","created_at":"2026-03-21T11:46:41Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-edz.2","depends_on_id":"jri-edz.1","type":"blocks","created_at":"2026-03-21T11:46:41Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"jri-edz.1","title":"Initialize Python project with FastAPI, uvicorn, and project structure","description":"Create the Python project skeleton for JRI.\n\n## What to create\n\nDirectory structure:\n```\n~/jri/\n├── app/\n│   ├── __init__.py\n│   ├── main.py          # FastAPI app, CORS, lifespan, static mount\n│   ├── config.py         # Load env vars from ~/jri.env using python-dotenv\n│   ├── database.py       # SQLite connection via aiosqlite\n│   ├── models.py         # Pydantic models for API request/response\n│   └── routers/\n│       ├── __init__.py\n│       ├── auth.py       # GitHub OAuth endpoints\n│       ├── projects.py   # Project CRUD endpoints\n│       ├── chat.py       # Ralphy chat endpoints\n│       ├── ralph.py      # Ralph loop control endpoints\n│       ├── uploads.py    # File upload management endpoints\n│       └── sse.py        # SSE streaming endpoints\n├── static/\n│   └── (empty for now, will hold frontend assets)\n├── templates/\n│   └── (empty for now, will hold Jinja2 templates)\n├── requirements.txt\n├── NOTES.md\n├── AGENTS.md\n└── README.md\n```\n\n## requirements.txt contents\n```\nfastapi==0.115.0\nuvicorn[standard]==0.30.0\naiosqlite==0.20.0\npython-dotenv==1.0.1\nhttpx==0.27.0\nitsdangerous==2.2.0\npython-multipart==0.0.9\njinja2==3.1.4\nmarkdown==3.7\nstripe==10.0.0\n```\n\n## app/config.py behavior\n- Load all env vars from ~/jri.env using python-dotenv\n- Expose them as module-level constants: GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET, SECRET_KEY, STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY\n- Also expose: DATA_DIR = Path.home() / 'jri' / 'data', RALPH_BOT_GITHUB_TOKEN (read from `gh auth token --hostname github.com` subprocess at startup, cached)\n\n## app/main.py behavior\n- Create FastAPI app with title 'Just Ralph It'\n- On startup lifespan: ensure DATA_DIR exists, initialize database\n- Mount static files at /static\n- Include all routers\n- No CORS needed (same-origin, served behind nginx)","acceptance_criteria":"1. Running `pip install -r requirements.txt` succeeds with no errors.\n2. Running `cd ~/jri \u0026\u0026 python -c 'from app.main import app; print(app.title)'` prints 'Just Ralph It'.\n3. Running `cd ~/jri \u0026\u0026 python -c 'from app.config import GITHUB_CLIENT_ID, SECRET_KEY, DATA_DIR; print(GITHUB_CLIENT_ID); print(DATA_DIR)'` prints the values from ~/jri.env and the correct data directory path.\n4. All files listed in the directory structure above exist.\n5. requirements.txt contains exactly the packages listed above (versions may be adjusted if needed for compatibility, but all packages must be present).\n6. Every .py file is valid Python (no syntax errors).","status":"open","priority":0,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:46:25Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:46:25Z","dependencies":[{"issue_id":"jri-edz.1","depends_on_id":"jri-edz","type":"parent-child","created_at":"2026-03-21T11:46:25Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"id":"jri-dyl","title":"Deployment \u0026 Nginx Configuration","description":"Deploy the app behind nginx on justralph.it, with the static page as fallback.","acceptance_criteria":"App is accessible at https://justralph.it, served by uvicorn behind nginx. Existing static page serves as fallback for unmatched routes. SSE streaming works through nginx (proper proxy headers). The app starts automatically on server boot.","status":"open","priority":0,"issue_type":"epic","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:46:00Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:46:00Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"jri-qps","title":"Ralph Loop","description":"The automated Ralph builder loop: issue pickup, TDD execution, commit/push, crash recovery, stop/resume controls.","acceptance_criteria":"Clicking JRI starts the Ralph loop. Ralph picks issues via bd ready -n 1 --json, claims them, reads AGENTS.md + issue details, writes tests first, implements, commits with Co-authored-by, pushes, closes issue. Loop continues until no ready issues. Crash recovery via git reset + reopen. Stop/Resume buttons work. Ralph tab streams stdout via SSE.","status":"open","priority":0,"issue_type":"epic","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:45:41Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:45:41Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"jri-aor","title":"Right Panel: Issues, AGENTS.md, and Uploads Tabs","description":"The right-side panel with tabs for live issue view, AGENTS.md rendering, and file uploads management.","acceptance_criteria":"Right panel shows 4 tabs. Issues tab: read-only, grouped by epic, expandable, polls bd list --json. AGENTS.md tab: rendered markdown, auto-refreshes. Uploads tab: file manager for \u003cproject\u003e/uploads/ (list, upload any file, delete, rename, name-collision handling with suffix). Ralph tab appears after JRI is clicked.","status":"open","priority":0,"issue_type":"epic","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:45:34Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:45:34Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"jri-2ti","title":"Ralphy Chat Interface","description":"The chat interface powering Ralphy (the interviewer agent), including message streaming, file attachments, and session persistence.","acceptance_criteria":"Users can chat with Ralphy in real-time with streaming responses. Ralphy maintains conversation history across page reloads. Chat attachments (images/PDFs, max 3MB) are processed multimodally. Input is disabled while Ralphy is processing.","status":"open","priority":0,"issue_type":"epic","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:45:21Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:45:21Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"jri-upe","title":"Project Management","description":"CRUD operations for projects: creation (with GitHub repo), dashboard listing, deletion.","acceptance_criteria":"Users can create projects (which creates a GitHub repo via ralphpujia, adds user as collaborator, initializes bd+git), view them on a dashboard, and delete them (optionally deleting the GitHub repo).","status":"open","priority":0,"issue_type":"epic","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:45:15Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:45:15Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"jri-6j8","title":"Authentication \u0026 User Management","description":"GitHub OAuth login flow, session management, and user persistence.","acceptance_criteria":"Users can sign in with GitHub, their profile is stored in SQLite, sessions persist across page reloads via signed cookies, and logout works.","status":"open","priority":0,"issue_type":"epic","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:45:10Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:45:10Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"jri-edz","title":"Project Foundation","description":"Set up the foundational project structure, dependencies, configuration, and database schema that all other features build upon.","acceptance_criteria":"Project runs with 'uvicorn app.main:app' from ~/jri/, serves on 127.0.0.1:8000, and has a working SQLite database at ~/jri/data/jri.db with users and projects tables.","status":"open","priority":0,"issue_type":"epic","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:45:05Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:45:05Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"jri-0tq.2","title":"Implement in-app notification system for Ralph blockers","description":"Implement the notification system for when Ralph is blocked and needs human help.\n\n## How notifications are created\nRalph's system prompt instructs it to create a beads issue assigned to 'Human' when blocked. The background issue poller (from the SSE event bus task) detects new issues with assignee='Human' and creates a notification.\n\n## Detection logic (in the background poller)\nWhen polling bd list --json, check for issues where:\n- assignee == 'Human'\n- status == 'open'  \n- The issue ID is not already associated with an acknowledged notification in SQLite.\n\nFor each such issue, insert a row into the notifications table and publish a 'notification' SSE event.\n\n## API endpoints (in app/routers/ralph.py)\n\n### GET /api/projects/{name}/notifications\n- Requires auth.\n- Return all unacknowledged notifications for this project.\n- Return JSON array of notification objects with id, message, beads_issue_id, created_at.\n\n### POST /api/projects/{name}/notifications/{id}/acknowledge  \n- Requires auth.\n- Set acknowledged=1 in SQLite.\n- Return 200.\n\n## SSE event format\nEvent type: notification. Data: {id, message, beads_issue_id, created_at}. The message should include the issue title for context. The frontend displays this as a persistent banner at the top of the page until acknowledged.","acceptance_criteria":"1. When Ralph creates an issue assigned to 'Human', a notification appears in the SSE stream within 3 seconds (one poll cycle).\n2. GET /api/projects/{name}/notifications returns unacknowledged notifications.\n3. POST /api/projects/{name}/notifications/{id}/acknowledge marks it as acknowledged.\n4. After acknowledging, the notification no longer appears in GET.\n5. The same Human-assigned issue does not create duplicate notifications.\n6. The notification message includes the issue title for context.","status":"open","priority":1,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:51:42Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:51:42Z","dependencies":[{"issue_id":"jri-0tq.2","depends_on_id":"jri-0tq","type":"parent-child","created_at":"2026-03-21T11:51:41Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-0tq.2","depends_on_id":"jri-qps.1","type":"blocks","created_at":"2026-03-21T11:51:41Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-0tq.2","depends_on_id":"jri-qps.2","type":"blocks","created_at":"2026-03-21T11:51:41Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"id":"jri-ehk.1","title":"Implement Stripe checkout for per-project billing","description":"Implement Stripe checkout flow triggered by the 'Just Ralph It' button.\n\n## Flow\n1. User clicks JRI button in the UI.\n2. Frontend calls POST /api/projects/{name}/ralph/checkout.\n3. Backend creates a Stripe Checkout Session.\n4. Backend returns the checkout URL.\n5. Frontend redirects to Stripe.\n6. On success, Stripe redirects to /project/{name}?payment=success.\n7. Backend verifies the payment via webhook or redirect handler, then starts the Ralph loop.\n\n## POST /api/projects/{name}/ralph/checkout\n- Requires auth.\n- Check free tier: count user's projects that have stripe_payment_id set. If 0 and this project has no payment, this is the free project — skip payment, start Ralph directly, return {\"free\": true, \"redirect\": null}.\n- Otherwise: create Stripe Checkout Session:\n  - mode: 'payment'\n  - line_items: [{ price_data: { currency: 'usd', unit_amount: 2000 (i.e., $20.00), product_data: { name: 'Just Ralph It — {project_name}' } }, quantity: 1 }]\n  - success_url: 'https://justralph.it/project/{name}?payment=success\u0026session_id={CHECKOUT_SESSION_ID}'\n  - cancel_url: 'https://justralph.it/project/{name}?payment=cancel'\n  - client_reference_id: str(project_id)\n  - metadata: { user_id: str(user_id), project_name: name }\n- Return {\"free\": false, \"redirect\": checkout_session.url}\n\n## GET /api/projects/{name}/ralph/payment-callback?session_id=...\n- Called when user returns from Stripe success URL.\n- Retrieve the Stripe session via stripe.checkout.Session.retrieve(session_id).\n- Verify payment_status == 'paid'.\n- Verify client_reference_id matches the project.\n- Store the session ID in projects.stripe_payment_id.\n- Start the Ralph loop.\n- Return 200 {\"status\": \"started\"}.\n- If payment not confirmed, return 402 {\"error\": \"Payment not confirmed\"}.\n\n## Configuration\n- stripe.api_key = STRIPE_SECRET_KEY (set in config.py)\n- Note: STRIPE_SECRET_KEY in jri.env currently has a pk_test_ prefix which is wrong. The code should use it as-is but log a warning if it starts with 'pk_' since that's the publishable key, not the secret key.","acceptance_criteria":"1. POST /api/projects/{name}/ralph/checkout for the first project (free tier) returns {\"free\": true} and starts Ralph directly.\n2. POST /api/projects/{name}/ralph/checkout for subsequent projects returns a Stripe checkout URL.\n3. After successful Stripe payment, GET /api/projects/{name}/ralph/payment-callback with valid session_id starts the Ralph loop and stores stripe_payment_id.\n4. GET payment-callback with an unpaid session returns 402.\n5. The Stripe checkout session has the correct amount ($20), product name, and redirect URLs.\n6. A warning is logged if STRIPE_SECRET_KEY starts with 'pk_'.","status":"open","priority":1,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:51:20Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:51:20Z","dependencies":[{"issue_id":"jri-ehk.1","depends_on_id":"jri-ehk","type":"parent-child","created_at":"2026-03-21T11:51:19Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-ehk.1","depends_on_id":"jri-qps.1","type":"blocks","created_at":"2026-03-21T11:51:19Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"jri-aor.3","title":"Implement uploads file manager API","description":"Create endpoints for managing files in the project's uploads/ directory.\n\n## GET /api/projects/{name}/uploads\n- Requires auth. Verify project belongs to user.\n- List all files in {project_dir}/uploads/.\n- Return JSON array: [{ \"name\": \"filename.ext\", \"size\": 12345, \"modified_at\": \"2024-01-01T00:00:00\" }, ...]\n- Sort by modified_at descending (newest first).\n- If uploads/ doesn't exist, return [].\n\n## POST /api/projects/{name}/uploads\n- Requires auth. Multipart/form-data with field `file` (single file upload).\n- No constraints on file type or size (unlike chat attachments).\n- Save to {project_dir}/uploads/{filename}.\n- **Name collision handling**: if a file with the same name exists, append a numeric suffix: `file.txt` → `file_1.txt` → `file_2.txt`. Find the next available suffix by scanning existing files.\n- Return JSON: { \"name\": \"actual_saved_filename.ext\", \"size\": 12345 }\n\n## DELETE /api/projects/{name}/uploads/{filename}\n- Requires auth.\n- Delete the file at {project_dir}/uploads/{filename}.\n- Return 404 if file doesn't exist.\n- Return 204 on success.\n- **Security**: validate that filename doesn't contain path traversal (.. or /). Return 400 if it does.\n\n## PATCH /api/projects/{name}/uploads/{filename}\n- Requires auth.\n- Request body: { \"new_name\": string }\n- Rename the file within uploads/.\n- **Security**: validate both old and new names don't contain path traversal.\n- If new_name already exists, apply the same collision suffix logic as POST.\n- Return JSON: { \"name\": \"actual_new_name.ext\" }\n- Return 404 if original file doesn't exist.","acceptance_criteria":"1. POST a file to /api/projects/{name}/uploads saves it to the uploads/ directory.\n2. POST the same filename again saves it as filename_1.ext (not overwriting).\n3. POST a third time with the same name saves as filename_2.ext.\n4. GET /api/projects/{name}/uploads lists all files with name, size, and modified_at.\n5. DELETE /api/projects/{name}/uploads/{name} removes the file and returns 204.\n6. DELETE a nonexistent file returns 404.\n7. PATCH /api/projects/{name}/uploads/{name} with new_name renames the file.\n8. Attempting path traversal (e.g., filename='../../etc/passwd') returns 400.\n9. All endpoints return 404 for projects that don't belong to the user.","status":"open","priority":1,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:49:43Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:49:43Z","dependencies":[{"issue_id":"jri-aor.3","depends_on_id":"jri-aor","type":"parent-child","created_at":"2026-03-21T11:49:42Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-aor.3","depends_on_id":"jri-upe.1","type":"blocks","created_at":"2026-03-21T11:49:43Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"jri-aor.2","title":"Implement AGENTS.md reading endpoint","description":"Create an endpoint to read the project's root AGENTS.md.\n\n## GET /api/projects/{name}/agents-md\n- Requires auth. Verify project belongs to user.\n- Read the file at {project_dir}/AGENTS.md.\n- If the file doesn't exist, return {\"content\": \"\", \"exists\": false}.\n- If it exists, return {\"content\": \"\u003craw markdown string\u003e\", \"exists\": true}.\n- The frontend will render this as HTML using a markdown library.","acceptance_criteria":"1. GET /api/projects/{name}/agents-md for a project with an AGENTS.md returns the raw markdown content.\n2. The response includes an 'exists' boolean field.\n3. For a project without AGENTS.md, returns empty content and exists=false.\n4. For an invalid project, returns 404.","status":"open","priority":1,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:49:28Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:49:28Z","dependencies":[{"issue_id":"jri-aor.2","depends_on_id":"jri-aor","type":"parent-child","created_at":"2026-03-21T11:49:27Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-aor.2","depends_on_id":"jri-upe.1","type":"blocks","created_at":"2026-03-21T11:49:28Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"jri-2ti.2","title":"Implement chat file attachments for multimodal processing","description":"Add file attachment support to the chat endpoint.\n\n## POST /api/projects/{name}/chat (updated)\nChange from JSON body to multipart/form-data:\n- Field `message`: string (required)\n- Field `attachments`: file upload(s) (optional, max 3 files)\n\n### Attachment validation\n- Allowed MIME types: image/png, image/jpeg, image/gif, image/webp, application/pdf\n- Max file size: 3MB per file\n- Return 400 with specific message if validation fails (wrong type or too large)\n\n### Attachment handling\n- For each valid attachment, convert to base64 and include in the claude CLI prompt as a multipart message.\n- Claude CLI's -p flag accepts content via stdin. The message format for multimodal should include the files.\n- Actually, claude CLI with -p does not natively support multimodal stdin. Instead: save the attachment to a temp file, and instruct Ralphy to read it using the Read tool. Prepend to the user message: 'The user attached the following file(s): {filename1} (saved at {temp_path1}), {filename2} (saved at {temp_path2}). Please read and analyze them.'\n- Save temp files to {project_dir}/.tmp_attachments/ and clean up after the response is complete.\n\n### Frontend considerations\n- The frontend will send this as FormData, not JSON. The chat endpoint must accept both:\n  - Content-Type: application/json (message only, no attachments)\n  - Content-Type: multipart/form-data (message + optional attachments)","acceptance_criteria":"1. POST with multipart/form-data containing a message and a PNG image returns a streaming response where Ralphy acknowledges the image content.\n2. POST with a PDF attachment works similarly — Ralphy can read and reference the PDF content.\n3. POST with a 4MB file returns 400 with message about size limit.\n4. POST with a .zip file returns 400 with message about allowed types.\n5. POST with JSON body (no attachments) still works as before.\n6. Temp files are cleaned up after the response stream ends.\n7. POST with 4 files returns 400 (max 3).","status":"open","priority":1,"issue_type":"task","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:49:08Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:49:08Z","dependencies":[{"issue_id":"jri-2ti.2","depends_on_id":"jri-2ti","type":"parent-child","created_at":"2026-03-21T11:49:08Z","created_by":"Nicolás Pujia","metadata":"{}"},{"issue_id":"jri-2ti.2","depends_on_id":"jri-2ti.1","type":"blocks","created_at":"2026-03-21T11:49:08Z","created_by":"Nicolás Pujia","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"jri-0tq","title":"In-App Notifications \u0026 Human Blocking","description":"When Ralph is blocked (needs human identity, API keys, etc.), it creates a Human-assigned issue and shows a persistent in-app banner.","acceptance_criteria":"When Ralph cannot proceed due to needing human help, it creates a beads issue assigned to 'Human' that blocks the current issue. A persistent banner appears in the UI until the user acknowledges. Ralph moves to the next iteration.","status":"open","priority":1,"issue_type":"epic","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:45:54Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:45:54Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"jri-ehk","title":"Stripe Integration \u0026 Billing","description":"Stripe checkout for per-project billing. Flat $20/project for v1. Free tier: 1 project.","acceptance_criteria":"Clicking JRI redirects to Stripe checkout ($20). On successful payment, Ralph loop starts. Free tier users get 1 project without payment. Payment failure shows error, Ralph does not start.","status":"open","priority":1,"issue_type":"epic","owner":"nicopujia.dev@gmail.com","created_at":"2026-03-21T11:45:47Z","created_by":"Nicolás Pujia","updated_at":"2026-03-21T11:45:47Z","dependency_count":0,"dependent_count":0,"comment_count":0}
